# 顶部菜单功能实现准备清单

## 一、总体说明

当前原型中的「文件 / 编辑 / 视图 / 插入 / 格式 / 帮助」六个菜单目前为纯 UI 占位，尚未绑定真实业务逻辑。要实现这些功能，需要补齐以下五类资料：

1. **业务需求与功能清单**（每个菜单项要做什么）
2. **数据结构与存储方案**（文档、页面、元素、历史记录如何存）
3. **后端接口/服务设计**（保存、导出、导入、翻译、协作等）
4. **第三方集成/文件格式处理**（PDF/Word/Excel/PPT 解析与导出）
5. **UI/UX 交互规范**（菜单禁用态、快捷键、弹窗、异常提示）

---

## 二、各菜单所需资料明细

### 1. 文件（File）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 新建项目 | 项目模板列表、默认页面尺寸（A4/A5/自定义）、空白项目初始化规则 |
| 打开项目 | 项目列表接口、项目搜索/排序/分页规则、最近打开记录（LocalStorage/后端） |
| 保存/另存为 | 项目保存接口、自动保存策略（定时/事件触发）、保存冲突处理规则 |
| 导入文档 | 支持的文件格式（PDF/Word/Excel/PPT/图片）、文件解析服务、导入后元素映射规则 |
| 导出文档 | 导出格式清单、导出模板（PDF/Word/Excel/PPT）、导出后文件下载方式 |
| 打印 | 打印样式/页眉页脚、浏览器打印 API 封装、打印预览规则 |
| 分享/发布 | 分享权限（公开/私密/密码）、分享链接生成、发布版本管理 |

**关键资料**：
- 项目数据模型（project / page / element / version）
- 导入导出格式映射表
- 文件存储方案（本地 IndexedDB / 后端对象存储）

---

### 2. 编辑（Edit）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 撤销/重做 | 操作历史栈数据结构（Command 模式）、撤销粒度规则 |
| 剪切/复制/粘贴 | 剪贴板协议（系统剪贴板/自定义 JSON 剪贴板）、跨页面粘贴规则 |
| 克隆 | 元素深拷贝规则、ID 重置策略 |
| 删除 | 删除确认策略、批量删除规则 |
| 全选 | 当前页面全选范围、多选交互规范 |
| 查找替换 | 查找范围（当前页/全文）、正则支持、替换后同步规则 |
| 属性 | 全局默认属性配置、批量编辑冲突处理 |

**关键资料**：
- Command/Action 数据模型（actionType / payload / inverseAction）
- 元素序列化格式（用于复制粘贴）
- 快捷键映射表

---

### 3. 视图（View）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 放大/缩小 | 缩放级别表（25%/50%/75%/100%...300%）、缩放中心点规则 |
| 适应窗口 | 画布容器尺寸计算、自适应算法 |
| 网格 | 网格间距（10px/20px）、吸附开关、网格样式 |
| 标尺 | 标尺单位（px/mm/cm）、标尺刻度绘制规则 |
| 预览模式 | 预览渲染器（隐藏编辑框/手柄）、翻页动画、预览页范围 |
| 多页/单页视图 | 页面切换模式、缩略图/大纲视图 |
| 显示/隐藏面板 | 面板状态持久化、默认布局 |

**关键资料**：
- 视图配置模型（zoom / grid / ruler / panelVisibility）
- 画布坐标系与单位换算规则

---

### 4. 插入（Insert）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 文本框 | 默认文本样式、富文本/纯文本模式 |
| 图片 | 图片上传接口、图片压缩/裁剪规则、图片资源库 |
| 表格 | 表格初始化行列数、表格样式、单元格合并规则 |
| 形状 | 形状库（矩形/圆形/线条/箭头）、默认样式 |
| 图表 | 图表类型（柱状/饼图/折线）、数据输入方式、图表渲染库 |
| 页眉页脚/页码 | 页眉页脚模板、页码格式、奇偶页规则 |
| 封面/目录/章节 | 说明书组件模板、自动目录生成规则 |
| 二维码/条形码 | 编码规则、生成库 |

**关键资料**：
- 组件元数据表（componentType / defaultProps / icon / category）
- 媒体资源管理方案（上传/存储/CDN）
- 说明书专用模板库

---

### 5. 格式（Format）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 字体/字号/颜色 | 字体列表（中文字体授权）、颜色板/主题色 |
| 加粗/斜体/下划线 | 文本样式模型、快捷键 |
| 对齐/缩进 | 对齐方式、段落缩进规则 |
| 行距/字距 | 行高倍数、字间距单位 |
| 项目符号/编号 | 列表样式库、嵌套规则 |
| 样式刷/清除格式 | 格式复制规则、默认样式回退 |
| 主题样式 | 全局主题配置（颜色/字体/间距）、一键换肤 |

**关键资料**：
- 文本样式模型（fontFamily / fontSize / color / bold / italic / lineHeight ...）
- 主题配置表（theme / styles / defaultColors）

---

### 6. 帮助（Help）

| 功能 | 需要准备的资料/能力 |
|------|-------------------|
| 使用文档 | 帮助中心内容、快捷键清单、操作视频/GIF |
| 快捷键 | 快捷键绑定表、快捷键冲突检测 |
| 关于 | 版本号、更新日志、版权信息 |
| 反馈 | 反馈入口、用户信息收集字段 |
| 新手引导 | 引导步骤、气泡提示、首次进入检测 |

**关键资料**：
- 帮助文档内容
- 版本信息配置文件
- 用户行为埋点方案

---

## 三、技术实现层面需要准备的资料

### 前端
- 组件渲染引擎（Fabric.js / Canvas / DOM）选型与封装方案
- 状态管理（Redux / Zustand / 自定义 Store）
- 事件系统（拖拽、缩放、选中、键盘、右键菜单）
- 撤销重做 Command 模式实现
- 导入导出 SDK（PDF.js / mammoth.js / xlsx / pptxgenjs）

### 后端
- 项目/用户/文件存储服务 API
- 文档转换服务（Python/Node + LibreOffice/专用库）
- 用户权限与协作锁机制
- 版本控制与历史记录存储
- 文件导出异步队列（大文档避免超时）

### 数据模型
- 项目（Project）：id / name / userId / pages / settings / createdAt / updatedAt
- 页面（Page）：id / index / elements / background / size
- 元素（Element）：id / type / x / y / width / height / rotation / style / content / zIndex
- 历史记录（History）：id / projectId / action / timestamp / snapshot
- 模板（Template）：id / name / category / thumbnail / pages / defaultLang

---

## 四、建议按此优先级推进

1. **先补齐「文件」和「编辑」**：这是编辑器的生命线，决定能否真正保存和修改内容。
2. **再做「视图」和「格式」**：提升编辑体验，属于锦上添花但不可或缺。
3. **然后做「插入」扩展**：将基础组件库和说明书专用模板落地。
4. **最后做「帮助」和协作**：上线前完善即可。

---

## 五、你可以先准备的资料

如果你希望快速推进，建议先整理以下 5 份资料：

1. **功能清单表**：每个菜单项下具体要哪些子功能，哪些先做哪些后做。
2. **数据字典**：项目、页面、元素、历史记录、模板的字段定义。
3. **接口清单**：需要后端提供的接口名称、入参、出参、异常码。
4. **导入导出格式说明**：支持哪些文件格式，导入后如何拆分为元素，导出时如何渲染。
5. **组件规范**：每种组件的默认属性、可编辑属性、渲染方式、快捷键。

有了这 5 份资料，前端就可以开始绑定菜单功能，后端可以并行开发接口。
