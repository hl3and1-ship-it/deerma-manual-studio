# 说明书制作线上系统 - 技术架构设计文档

## 一、项目概述

### 1.1 项目定位
一个全平台覆盖的在线可视化说明书制作系统，参考翻译排版大师的编辑体验，支持 PDF、Word、Excel、PPT 全格式文档处理，提供多语言版本管理能力。

### 1.2 核心目标
- 提供类似翻译排版大师的流畅编辑体验
- 支持多文档格式的导入、编辑和导出
- 实现说明书的专业排版功能
- 支持多语言版本的一键翻译和管理

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐           │
│  │   Web 编辑器  │  │  移动端 H5   │  │   小程序端   │           │
│  └─────────────┘  └─────────────┘  └─────────────┘           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        API 网关层                                │
│  认证授权 │ 负载均衡 │ 限流熔断 │ 请求路由                        │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   文档处理服务    │ │   模板管理服务    │ │   协作版本服务   │
└─────────────────┘ └─────────────────┘ └─────────────────┘
          │                   │                   │
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│   导出转换服务    │ │   翻译服务集成   │ │   用户权限服务   │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 2.2 技术栈选型

#### 前端技术栈
| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | React 18 + TypeScript | 稳定的企业级框架 |
| 可视化编辑器 | Fabric.js / Konva.js | Canvas 编辑器核心 |
| 状态管理 | Zustand / Redux Toolkit | 轻量级状态管理 |
| UI 组件库 | Ant Design 5 / Chakra UI | 企业级组件库 |
| 构建工具 | Vite 4 | 快速的开发体验 |
| 文档解析 | pdf.js / mammoth.js | PDF/Word 解析 |
| 导出引擎 | pdf-lib / docx | 文档生成 |

#### 后端技术栈
| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | Spring Boot 3 / NestJS | 企业级后端框架 |
| 语言 | Java 17+ / Node.js 18+ | 高性能运行时 |
| 数据库 | PostgreSQL + MongoDB | 关系型+文档型混合 |
| 缓存 | Redis Cluster | 高性能缓存 |
| 文件存储 | OSS / S3 | 对象存储服务 |
| 搜索引擎 | Elasticsearch | 全文检索 |
| 消息队列 | RabbitMQ / Kafka | 异步任务处理 |
| CDN | 阿里云 CDN / Cloudflare | 静态资源加速 |

#### 部署架构
| 类别 | 技术选型 | 说明 |
|------|---------|------|
| 容器化 | Docker + Kubernetes | 容器编排 |
| CI/CD | GitLab CI / Jenkins | 持续集成部署 |
| 日志 | ELK Stack | 日志收集分析 |
| 监控 | Prometheus + Grafana | 监控告警 |

---

## 三、功能模块设计

### 3.1 核心功能模块

#### 3.1.1 可视化编辑器模块
**功能描述**：提供拖拽式的可视化编辑体验

**子模块**：
- 画布管理：缩放、平移、网格吸附、对齐辅助线
- 组件操作：选择、移动、缩放、旋转、复制粘贴、撤销重做
- 分组与锁定：组件分组、图层管理、锁定保护
- 快捷键支持：常用操作快捷键绑定

#### 3.1.2 组件库模块
**功能描述**：提供丰富的排版组件

**组件分类**：

| 类别 | 组件列表 |
|------|---------|
| 基础组件 | 文本框、图片、形状、表格、图表、图标 |
| 说明书专用 | 封面、目录、章节标题、页眉页脚、编号列表、注意事项、警告框 |
| 高级组件 | 目录生成器、索引生成器、交叉引用、术语表 |

#### 3.1.3 文档导入导出模块
**功能描述**：支持多种文档格式的导入导出

**支持格式**：
- 导入：PDF、Word (.docx/.doc)、Excel (.xlsx)、PPT (.pptx)、图片 (PNG/JPG/SVG)
- 导出：PDF、Word、Excel、PPT、HTML、图片

**处理流程**：
```
上传文件 → 格式识别 → 内容解析 → 格式转换 → 编辑保存 → 导出生成
```

#### 3.1.4 多语言管理模块
**功能描述**：支持说明书的多语言版本管理

**功能点**：
- 语言版本列表：查看所有翻译版本
- 术语库管理：统一专业术语翻译
- 翻译进度追踪：显示各语言完成度
- 智能翻译：集成 AI 翻译能力
- 版本对照：原文与译文对照校对

#### 3.1.5 模板管理模块
**功能描述**：提供丰富的模板支持

**模板类型**：
- 官方模板：产品说明书、技术手册、用户指南
- 行业模板：制造业、医疗器械、电子产品
- 自定义模板：用户创建的模板库

#### 3.1.6 协作版本管理模块
**功能描述**：支持多人协作编辑

**功能点**：
- 实时协作：多人同时编辑
- 版本历史：查看和恢复历史版本
- 评论批注：添加评论和批注
- 权限管理：细粒度的操作权限控制

---

## 四、界面布局设计

### 4.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  Logo  │  文件  │  编辑  │  视图  │  插入  │  格式  │   预览  │ 导出 │ │
├────────┴────────┴────────┴────────┴────────┴────────┴────────┴────┤
│        │                                    │                    │
│  组件库 │           编辑画布区域              │      属性面板      │
│        │                                    │                    │
│ 搜索组件 │    ┌─────────────────────────┐   │  位置 / 尺寸        │
│        │    │                         │   │  字体 / 颜色        │
│ 基础组件 │    │     说明书页面预览        │   │  边框 / 背景        │
│ - 文本框 │    │                         │   │  动画 / 交互        │
│ - 图片   │    │     (拖拽组件到此处)      │   │                    │
│ - 表格   │    │                         │   ├────────────────────│
│ - 形状   │    │                         │   │  多语言版本管理      │
│        │    └─────────────────────────┘   │  中文 │ EN │ 日语   │
│ 说明书组件 │                                  │                    │
│ - 封面    │                                  │                    │
│ - 目录    │                                  │                    │
│ - 章节标题 │                                  │                    │
├─────────┴──────────────────────────────────┴─────────────────────┤
│  缩放: 100%  │  页码: 1/12  │  多语言: 中文  │  自动保存: 2分钟前   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 功能区域说明

| 区域 | 功能 | 描述 |
|------|------|------|
| 顶部菜单栏 | 文件操作 | 新建、打开、保存、导出、打印 |
| 左侧组件库 | 组件拖拽 | 分类展示组件，支持搜索过滤 |
| 中央编辑区 | 画布编辑 | 主编辑区域，支持缩放平移 |
| 右侧属性面板 | 属性配置 | 选中组件的属性编辑 |
| 底部状态栏 | 状态信息 | 缩放比例、页码、语言、保存状态 |

---

## 五、数据模型设计

### 5.1 核心实体

#### Document (文档)
```typescript
interface Document {
  id: string;
  name: string;
  description?: string;
  pages: Page[];
  templates: string[];
  createdAt: Date;
  updatedAt: Date;
  owner: string;
  collaborators: Collaborator[];
  languages: LanguageVersion[];
  status: 'draft' | 'review' | 'published';
}
```

#### Page (页面)
```typescript
interface Page {
  id: string;
  pageNumber: number;
  width: number;
  height: number;
  backgroundColor?: string;
  backgroundImage?: string;
  elements: Element[];
  pageSettings: PageSettings;
}
```

#### Element (元素/组件)
```typescript
interface Element {
  id: string;
  type: ElementType;
  position: { x: number; y: number };
  size: { width: number; height: number };
  rotation: number;
  style: ElementStyle;
  content: ElementContent;
  locked: boolean;
  visible: boolean;
  groupId?: string;
}
```

#### LanguageVersion (语言版本)
```typescript
interface LanguageVersion {
  languageCode: string;
  languageName: string;
  translatedAt?: Date;
  translator?: string;
  status: 'pending' | 'in_progress' | 'completed';
  progress: number;
  document: Document;
}
```

---

## 六、API 接口设计

### 6.1 文档管理 API

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/documents` | POST | 创建新文档 |
| `/api/documents/{id}` | GET | 获取文档详情 |
| `/api/documents/{id}` | PUT | 更新文档 |
| `/api/documents/{id}` | DELETE | 删除文档 |
| `/api/documents/{id}/pages` | GET | 获取页面列表 |
| `/api/documents/{id}/export` | POST | 导出文档 |

### 6.2 组件操作 API

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/documents/{id}/elements` | POST | 添加元素 |
| `/api/documents/{id}/elements/{eid}` | PUT | 更新元素 |
| `/api/documents/{id}/elements/{eid}` | DELETE | 删除元素 |
| `/api/documents/{id}/elements/batch` | POST | 批量操作 |

### 6.3 多语言 API

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/documents/{id}/languages` | GET | 获取语言版本列表 |
| `/api/documents/{id}/translate` | POST | 发起翻译任务 |
| `/api/documents/{id}/languages/{lang}` | GET | 获取指定语言版本 |
| `/api/documents/{id}/sync` | POST | 同步原文变更 |

### 6.4 导入导出 API

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/import` | POST | 导入文档 |
| `/api/import/parse` | POST | 解析文档内容 |
| `/api/export/{id}/pdf` | POST | 导出为 PDF |
| `/api/export/{id}/word` | POST | 导出为 Word |
| `/api/export/{id}/batch` | POST | 批量导出 |

---

## 七、开发路线图

### Phase 1: MVP 版本 (8-10 周)
- [ ] 可视化编辑器核心功能
- [ ] 基础组件库 (文本、图片、形状)
- [ ] 文档导入导出 (PDF/Word)
- [ ] 用户认证与项目管理

### Phase 2: 专业功能 (6-8 周)
- [ ] 说明书专用组件
- [ ] 模板系统
- [ ] 多语言管理
- [ ] 协作编辑基础功能

### Phase 3: 企业功能 (6-8 周)
- [ ] 完整的权限管理系统
- [ ] 版本历史与恢复
- [ ] 评论批注系统
- [ ] 企业级 API 集成

### Phase 4: 高级功能 (4-6 周)
- [ ] AI 辅助写作
- [ ] 智能翻译集成
- [ ] 移动端适配
- [ ] 性能优化

---

## 八、技术亮点

### 8.1 编辑器性能优化
- Canvas 渲染优化：分层渲染、脏矩形更新
- 大文档处理：虚拟化列表、懒加载
- 实时协作：CRDT 算法、增量同步

### 8.2 文档处理能力
- 高保真排版还原
- 复杂表格解析与重建
- 多媒体内容处理

### 8.3 多语言支持
- 术语库统一管理
- 上下文感知翻译
- RTL 语言支持

---

## 九、附录

### 9.1 参考资料
- 翻译排版大师: https://www.fanyipaiban.com
- Fabric.js 文档: https://fabricjs.com
- pdf-lib 文档: https://pdf-lib.org

### 9.2 术语表
| 术语 | 英文 | 说明 |
|------|------|------|
| 画布 | Canvas | 编辑区域的画布对象 |
| 元素 | Element | 编辑器中的可编辑对象 |
| 组件 | Component | 预设的 UI 组件 |
| 模板 | Template | 预设的文档样式 |
